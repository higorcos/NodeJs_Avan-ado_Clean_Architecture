/* import { MongoHelper as sut } from './monge-helper';

describe('MongoHelper', () => {
  beforeAll(async () => {
    await sut.connect(process.env.MONGO_URL);
  });

  afterAll(async () => {
    await sut.disconnect();
  }); */
/* 
  test('should reconnect if mongodb is down', async () => {
    Não tem necessidade de testar isso aqui o isConnect do mongo foi descontinuado 
    let accountCollection = sut.getCollection('accounts');
    expect(accountCollection).toBeTruthy();
    await sut.disconnect();
    accountCollection = sut.getCollection('accounts');
    expect(accountCollection).toBeTruthy();
  }); */
/* }); */
